# BusPerú · frontend · PLANTILLA DE PRODUCCIÓN (sin valores reales). Ver PRODUCCION.md §3.
# Se incrusta en el bundle al construir. `npm run build` falla si falta, si apunta a
# localhost o si no usa https. Alternativa detrás de un proxy del mismo dominio: /api
VITE_API_URL=
