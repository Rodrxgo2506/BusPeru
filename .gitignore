node_modules/
dist/
build/
.env
.env.local
# F15-01: cualquier .env.<entorno> real (p. ej. .env.production) queda fuera; solo las plantillas se versionan.
.env.*
!.env.example
!.env.*.example
*.log
logs/
.DS_Store
Thumbs.db
coverage/
.vite/
# F16A: caché incremental de `tsc -b` (se regenera en cada build del frontend). Ya versionado en 1d6cb2a:
# esta regla no lo saca del índice; hay que retirarlo con `git rm --cached` al preparar el commit.
*.tsbuildinfo
# F18-11B: paquetes que genera infra/aws/scripts/make-release.sh (se suben a S3; nunca se versionan).
infra/aws/.releases/

# F18-18: manifiestos locales de infra/aws/scripts/publish-web.mjs
web-releases/
